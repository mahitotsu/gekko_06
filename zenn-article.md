---
title: "stdio MCPサーバーの寿命 — Claude Agent SDKは子プロセスをいつ・どう終了させるか"
emoji: "🔌"
type: "tech"
topics: ["claudecode", "mcp", "typescript", "python", "uv"]
published: false
---

## この記事のきっかけ

Claude Agent SDK (TypeScript) で `mcpServers` に stdio 型の MCP サーバー(`uv run` で起動する Python サーバーなど)を登録すると、SDK は裏で `claude` CLI を子プロセスとして起動し、その CLI がさらに MCP サーバーをサブプロセスとして起動します。

では、そのセッションが終わったとき——正常終了でも、中断でも、あるいはホストプロセスがクラッシュしたときでも——この MCP サーバーの子プロセスは誰が、どうやって片付けるのでしょうか。

[Agent SDK の公式ドキュメント](https://code.claude.com/docs/en/agent-sdk/mcp)には、MCP サーバーの**接続**タイミングやステータス遷移は詳しく書かれている一方、**子プロセスをいつ・どうやって終了させるか**(シグナルの種類、タイミング、異常系での挙動)についての記載は見当たりません。そこで、この空白を実測で埋めることにしました。

## 前提知識:誰が何を呼んでいるのか

`query()` を呼んでも、自分の Node.js コードがすべてをやっているわけではありません。SDK は裏で**別プロセスとして `claude` コマンド本体を起動**し、標準入出力で JSON メッセージをやり取りする薄いラッパーです。stdio で MCP サーバーを繋ぐと、プロセスはさらに二段深くなります。

```
① あなたのNode.jsスクリプト (例: npx tsx myscript.ts)
    └─ ② claude CLIプロセス   ← 以後「CLI」と呼ぶ
         └─ ③ uv run main.py
              └─ ④ python3 main.py (MCPサーバー本体)
```

API通信・ツールの実行判断・MCPサーバーとの接続管理をしているのは①ではなく**②のCLI**です。①が消えても②は自動的には消えません——これが後半の伏線になります。

| 用語 | 意味 |
|---|---|
| `query()` | SDKが提供する関数。呼ぶたびに新しいCLIプロセスが1つ起動される |
| セッション | 1回の`query()`呼び出しに対応する、CLI⇔MCPサーバー間の一連のやり取り |
| CLI | SDKが裏で起動する`claude`コマンド本体(上図②) |
| stdio transport | クライアント(CLI)がサーバーをサブプロセスとして起動し、標準入出力でJSON-RPCをやり取りする通信方式 |
| SIGINT/SIGTERM | プロセスへ送る終了シグナル。ハンドラを登録して捕捉・無視できる |
| SIGKILL | 強制終了シグナル。カーネルレベルで強制され、捕捉も無視もできない唯一の手段 |
| 孤児プロセス | 親プロセスが死んだ後も生き残った子プロセス。init(PID 1)の下に付け替えられ、誰も後始末しなければ動き続ける |

## 検証方法:2軸を固定した構造化ハーネス

検証対象の軸は2つだけに絞りました。

- **Trigger(終了トリガー)**: `normal-completion`(正常完了) / `interrupt`(中断) / `subagent`(サブエージェント経由) / `kill-cli`(CLIを外部からSIGKILL) / `kill-node`(Node.jsホストを外部からSIGKILL)
- **ServerBehavior(MCPサーバーの行儀)**: `normal`(シグナル・EOFに素直に応答) / `ignore-signals`(シグナルだけ無視) / `ignore-signals-and-eof`(シグナルもEOFも無視して居座る)

この2軸**以外**の条件——ポーリング間隔(200ms)、終了トリガー発火後の観察時間(一律90秒)、ツール呼び出しの秒数(6秒)、許可ツール、`permissionMode`など——は1箇所の定数に固定し、全15シナリオで共有しています。同期も `sleep(N)` の当てずっぽうをやめ、MCPサーバー自身が書く構造化JSONLイベント(`work_started`など)を監視するイベント駆動方式にしました。

決定表は手で転記せず、`results/*.json` から機械的に生成しています。

:::message
最初はパターンごとにスクリプトをコピーして検証していたのですが、ポーリング間隔や観察時間がパターンごとに微妙に異なってしまい、「検証対象以外の条件を揃える」という基本から外れてしまいました。上記の構造化ハーネスは、それを作り直した結果です。
:::

## 結果:Trigger × ServerBehavior フルマトリクス(15/15)

| trigger | behavior | server(MCPサーバー) | cli(claudeプロセス) |
|---|---|---|---|
| normal-completion | normal | 死亡 (+0.00s) | n/a |
| normal-completion | ignore-signals | 死亡 (+0.00s) | n/a |
| normal-completion | ignore-signals-and-eof | 死亡 (+0.85s) | n/a |
| interrupt | normal | 死亡 (+0.00s) | n/a |
| interrupt | ignore-signals | 死亡 (+1.04s) | n/a |
| interrupt | ignore-signals-and-eof | 死亡 (+1.04s) | n/a |
| subagent | normal | 死亡 (+0.00s) | n/a |
| subagent | ignore-signals | 死亡 (+0.00s) | n/a |
| subagent | ignore-signals-and-eof | 死亡 (+0.83s) | n/a |
| kill-cli | normal | 死亡 (+6.19s) | 死亡 (+0.20s) |
| kill-cli | ignore-signals | 死亡 (+6.15s) | 死亡 (+0.20s) |
| kill-cli | ignore-signals-and-eof | 死亡 (+6.20s) | 死亡 (+0.20s) |
| kill-node | normal | 死亡 (+8.56s) | 死亡 (+8.96s) |
| kill-node | ignore-signals | 死亡 (+8.33s) | 死亡 (+8.74s) |
| kill-node | ignore-signals-and-eof | 死亡 (+13.50s) | 死亡 (+12.65s) |

(cli列の「+N秒」はトリガー発火からの経過時間。`kill-cli`/`kill-node`のcliはkill対象そのものなので、死亡検知は「本当にkillできたか」の確認。)

加えて、セッション/プロセスの対応関係も別枠で確認しました。メインNode.jsプロセスを終了させずに`query()`を2回連続実行すると、MCPサーバーは毎回**別PID**で立ち上がり直します。並列に2つ実行しても、2つのMCPサーバーが**同時刻に**独立して生存します。stdio transportである以上、「1 `query()` = 1 MCPサーバープロセス = 1セッション」が仕様上の必然だからです(後述)。

## 発見1: 正常系はどのトリガーでも実質0秒で終わる

行儀の良いサーバー(`normal`)は全トリガーで実質0秒(次のポーリングサンプルで既に死亡)。後始末のコードを自分で書く必要はありません。

サーバーに直接シグナルハンドラを仕込んで実測すると、**`SIGINT`と`SIGTERM`が1ミリ秒未満の差でほぼ同時に**届いていました。

[MCP公式仕様(2025-06-18)のLifecycle章](https://modelcontextprotocol.io/specification/2025-06-18/basic/lifecycle#shutdown)は、stdio transportのシャットダウン手順を次のように定めています。

> 1. まずサーバーへの入力ストリーム(stdin)を閉じる
> 2. サーバーの終了を待つ。合理的な時間内に終了しなければ`SIGTERM`
> 3. `SIGTERM`後も終了しなければ`SIGKILL`

実測では「様子見」の段階は観測されず、stdinのクローズも(シグナルと)並行して行われているようでした。仕様の"SHOULD"からは外れていますが、`SIGTERM`だけで毎回きれいに終了できている以上、実害のあるずれではなさそうです。

## 発見2: 行儀の悪いサーバーにも、多段の安全網がある

シグナルを無視するだけの`ignore-signals`は、**stdin EOFで自然終了**するため大きな遅れは出ません。CLIはシグナル送信と並行してstdinのクローズも行っているようです。

シグナルもEOFも無視する`ignore-signals-and-eof`(`mcp.run()`が自然リターンしても無限ループで再突入し続ける、最強に強情なサーバー)には、最終的に**`SIGKILL`**が飛んできます。詳細を追うと、シグナル送信から実際の強制終了まで約**5.5秒**でした。`SIGKILL`はカーネルレベルで強制されるシグナルで、プロセス側に打つ手はありません。

:::message
表では`normal-completion × ignore-signals-and-eof`が0.85秒と、この5.5秒よりずっと短く見えますが矛盾ではありません。CLIは**ターンの完了報告(`query()`の結果を返すこと)と、MCPサーバーの後始末を並行して**進めており、両者は同じ瞬間には終わりません。今回の表は「呼び出し側から見て`query()`が完了してから、実際にプロセスが消えるまで」を測っているため、たまたま`query()`の完了タイミングが後始末の終盤(強制終了の直前)と重なっていた、というのが実態です。**`query()`のfor-awaitループが終わった直後に「MCPサーバーはもう存在しない」と決めつけるのは、行儀の悪いサーバーの場合は安全ではありません。**
:::

## 発見3: `kill-cli`の死亡タイミングは「シグナル」ではなく「次にI/Oを試みるタイミング」で決まる

外部から`claude` CLIプロセスへ`kill -9`を撃ち込むと、3つの行儀すべてで、サーバーの死亡は**約6.15〜6.20秒後**とほぼ同一でした。これはワークツールの呼び出し秒数(6秒)とほぼ一致します。

イベントログを見ると、この経路ではシグナルは一切送られていません。サーバーは`time.sleep(6)`をブロッキングのまま最後まで実行し、完了後に結果を書き込もうとした瞬間に、壊れたパイプ(後述する理由で`uv`も道連れに死んでいる)を検知してクラッシュしています。

つまり「行儀が良いか悪いか」はこの経路では無関係です。`time.sleep()`のように何のI/Oも行わない処理を実行中は、シグナルハンドラの有無に関係なく、そのブロッキング処理が終わるまで異常を検知できません。**死亡タイミングは概ね「トリガー発火時点で残っていた処理時間」で決まります。**

### プロセスツリーで見る:`uv`という隠れた安全装置

```
claude (CLI, 外部からkill -9)
  └─ uv run --directory ... main.py   ← 自分の入出力が壊れたのを検知し、子を道連れに自滅
       └─ python3 main.py             ← MCPサーバー本体
```

`uv`自身のPIDを追跡すると、CLIが死んだ直後に`uv`と`python3`が**ほぼ同時(0.3秒以内)に消滅**していました。これは偶然ではなく、[uv公式ドキュメント](https://docs.astral.sh/uv/concepts/projects/run/)にも「`uv`は`SIGKILL`/`SIGCHLD`/`SIGIO`/`SIGPOLL`を除くほとんどのシグナルを子プロセスへ転送する」と明記されている、意図された挙動の一事例だと考えられます。

ただし万能ではありません。[astral-sh/uv #12830](https://github.com/astral-sh/uv/issues/12830)は、`SIGHUP`/`SIGQUIT`/`SIGILL`/`SIGTRAP`/`SIGABRT`などでは**uvが転送せず自分だけ終了し、Pythonプロセスを孤児のまま残す**バグを報告しています。「`uv`が常に安全装置になる」とは言い切れない点は留意が必要です。

## 発見4: `kill-node`——初期の結論を訂正する

今度はCLIの親、つまりNode.jsホストプロセス自体を外部から`kill -9`します。CLIより一段上を落とすだけなので、理屈上は`kill-cli`と同じ結果になりそうに思えました。

最初、観察時間を30〜60秒に区切った実験では、**CLIプロセスは一度も自己終了しませんでした**。「Node.jsホストがクラッシュすると、CLIは唯一の確実なプロセスリーク源として無期限に残り続ける」——そう結論づけかけたのですが、これは**観察時間が短すぎたことによる誤り**でした。

全シナリオに一律**90秒**の観察窓を適用したところ、CLIは`normal`/`ignore-signals`で約**8.7〜9.0秒後**、`ignore-signals-and-eof`でも約**12.7秒後**に、自分から終了していました。`ignore-signals-and-eof`だけやや長いのは、サーバー側の強制終了エスカレーション(約5.5秒)の分、余計に時間がかかっているためで、これも一貫した説明がつきます。

CLIはホストプロセスが死んで孤児になっても、進行中のツール呼び出しが終わるのを待ち、通常のターン完了処理(MCPサーバーへのシグナル送信、応答がなければ最終的な強制終了)を一通り済ませたうえで、自分自身も終了していました。**「孤児プロセスが無期限に残り続ける」というリスクは、少なくとも今回の90秒の観測範囲では確認されませんでした。**

ただし、これは**ツール呼び出しの実行中というCLIが必ず活動している瞬間**にホストを落とした場合の結果です。CLIが次のプロンプトを待つだけの**アイドル状態**でホストが死んだ場合や、1つのセッション内で多数のサブエージェントを使い込んだ場合の挙動までは検証しておらず、そこは別の結果になる可能性があります。

:::message alert
逆に言えば、`uv`という安全装置が効くのは、あくまで**CLIの直接の子**が異常を検知できる場合に限られます。Node.jsが死んでもCLIは(`uv`から見て)まだ生きている親であり続けるため、`uv`の安全装置はここでは発動しません。CLI自身が気づくかどうかは、その瞬間に何をしていたか次第です。
:::

## 検証ハーネス自体で見つかったバグ

`kill-cli`/`kill-node`トリガーでは、対象の`claude` CLIプロセスを最初はシステム全体から`pgrep -f "claude-agent-sdk-.*/claude"`で検索していました。ところがある実行で、**このテスト開始より前から存在していた、無関係な別セッションのCLIプロセス**を誤って検出・killしていたことが判明しました(この検証環境はVS Code拡張のホストプロセス上で動いており、他のセッションが同名バイナリを使っていることがあります)。

`ps -eo pid,ppid,cmd`からプロセスツリーをたどり、**自分が起動した子孫プロセスの中だけ**を探す方式に修正し、候補が1つに定まらない場合はkillを見送るようにしました。「検証対象以外の条件を揃える」という原則は、検証ハーネス自身の正しさにも適用する必要がある、という教訓です。

## MCP公式仕様・公式ドキュメントとの比較

[Transports章 Session Management節](https://modelcontextprotocol.io/specification/2025-06-18/basic/transports#session-management)によれば、"session"の対応関係はtransportごとに異なります。stdioではセッションIDという概念自体が存在せず、「1回のサブプロセス起動 = 1セッション」が構造的に固定されます。今回のトポロジー確認の結果(逐次・並列いずれも別PID)はこれと整合します。

Claude Code / Agent SDKの公式ドキュメントを確認したところ:

- **記載あり**: 「1 agent session = 1 subprocess」という粒度([Hosting the Agent SDK](https://code.claude.com/docs/en/agent-sdk/hosting)「The subprocess model」)、MCPサーバーの接続タイミングやステータス遷移([Connect to external tools with MCP](https://code.claude.com/docs/en/agent-sdk/mcp)「Connection timing」「Error handling」)
- **記載なし**: stdio MCPサーバーの子プロセスをいつ・どうやって終了させるか、ホストプロセスがクラッシュした場合の子プロセスの扱い

「1 query() = 1 subprocess」という設計思想は文書化されている一方、シグナルの種類・タイミング・異常系での挙動は今回のように自前のシグナルハンドラとプロセスツリー観察、構造化ハーネスによる検算でしか確かめられませんでした。

## 実務への示唆

- **正常系の後始末は自分で書く必要はない。** 行儀の良いサーバーなら、どのトリガーでも子プロセスは実質0秒で終了する。
- **多少行儀の悪いMCPサーバーでも、ほぼ大丈夫。** シグナルを無視するだけならstdin EOFが、それも無視するならCLIの最終手段(約5.5秒後のSIGKILL)が、さらにCLI自体が消えても`uv run`自身の安全装置(約1秒、ただしI/O待ち中に限る)が働く。
- **`query()`が返ってきた直後に「MCPサーバーはもう消えた」と決めつけない。** 行儀の悪いサーバー相手だと、後始末が数秒バックグラウンドで続いていることがある。
- **ブロッキングな長時間ツールは、親が死んでも自分では気づけない。** 死亡検知は「シグナル」ではなく「次にI/Oしようとするタイミング」で決まる。長時間処理を伴うツールを設計する際は、定期的なI/O(進捗通知など)を挟むと異常検知が速くなる可能性がある。
- **ホストプロセスがクラッシュしても、CLIは自分で片付けにいく。** ただし進行中の処理が終わるまで(長くて十数秒)は生き続ける。短命なコンテナやサーバーレス環境では、この十数秒すら惜しい場面もあるはずなので、プロセスグループ単位で確実に後始末する監督者を用意しておくと安心。
- **状態はセッション(=1回のquery())をまたいで残らない。** stdio transportである以上、これは実装の都合ではなく仕様上の必然。複数ターンにまたがる状態を持たせたいなら、MCPサーバー自体をStreamable HTTPの長命プロセスとして設計し直す必要がある。

## 再現方法

検証一式(Pythonサーバー・TypeScriptの構造化ハーネス・実測ログ)はリポジトリにコミット済みです。

```bash
cd tsclient
npm run matrix     # 全15組み合わせを実行(所要時間: 約15〜20分)
npm run topology    # セッション/プロセスの対応関係の確認
npm run render        # results/*.json から決定表(Markdown)を生成
```

SSE/HTTP型MCPサーバーでの挙動、`interrupt`/`subagent`/逐次・並列セッションと`kill-cli`/`kill-node`の組み合わせ(計10セル、既に検証した独立要因の組み合わせと判断し対象外)、**CLIがアイドル状態(ツール呼び出しが何もない状態)でホストが落ちた場合**、**1つのセッション内で多数のサブエージェントを使い込んだ場合のMCP接続の蓄積**は、今回のマトリクスには含めていません。

## まとめ

- **正常系・多少の異常系は、SDK側が自動的に後始末してくれる。** 何重かの安全網(シグナル、stdin EOF、`uv`の子プロセス連鎖終了、CLIの最終手段)が働いている。
- **唯一注意すべきは、進行中の処理がI/Oを行わないブロッキング処理のとき。** 死亡検知は処理が終わるまで遅れる。
- **ホストプロセスのクラッシュも、少なくとも「ツール呼び出し中に落ちた」場合は無期限のリークにはならない。** ただし十数秒のプロセス残存は起こりうるうえ、CLIがアイドル中にホストが落ちた場合やサブエージェントを多用した場合は未検証。短命な実行環境や本番運用では、プロセスグループ単位の監督者を用意しておくと安全。
- この領域は公式ドキュメントに書かれていないだけでなく、GitHub Issue上でも「現象の報告」止まりで、原因の分解や再現可能な検証はされていませんでした。本記事がその空白を埋める一助になれば幸いです。
