---
title: "stdio MCPサーバーの寿命 — Claude Agent SDKは子プロセスをいつ・どう終了させるか"
emoji: "🔌"
type: "tech"
topics: ["claudecode", "mcp", "typescript", "python", "uv"]
published: false
---

## この記事のきっかけ

TypeScript版のClaude Agent SDKで `mcpServers` に stdio 型の MCP サーバーを登録すると、SDK は裏で `claude` CLI を子プロセスとして起動します。その CLI が、さらに MCP サーバーをサブプロセスとして起動する、という構造です。この記事では、MCPサーバー側の実装として `uv run` で起動する Python サーバーを例に使います。

気になったのは、そのセッションが終わったときの後始末でした。正常終了でも、中断でも、あるいはホストプロセスがクラッシュしたときでも——この MCP サーバーの子プロセスは、誰が、どうやって片付けるのでしょうか。

[Agent SDK の公式ドキュメント](https://code.claude.com/docs/en/agent-sdk/mcp)を読んでみると、MCP サーバーの**接続**タイミングやステータス遷移は詳しく書かれています。一方で、子プロセスの終了のさせ方についての記載は見当たりませんでした。ここでいう「終了のさせ方」とは、シグナルの種類、タイミング、そして通常通りに終わらなかった場合にどうなるか、を指します。

そこで、この空白を実測で埋めてみることにしました。先に結論を言ってしまうと、**Claude Agent SDKでuvベースのstdio MCPサーバーは、特別な後始末コードを書かなくても安心して使えます**。セッションが正常に終わればMCPサーバーのプロセスもほぼ同時に終了しますし、サーバー側の実装が多少行儀の悪いものであっても、複数の仕組みが重なって最終的にはちゃんと片付きます。この記事では、その根拠になった実測データと、実装するうえで気をつけておきたいポイントを紹介します。

## 前提知識:誰が何を呼んでいるのか

`query()` を呼んでも、自分の Node.js コードがすべてをやっているわけではありません。SDK は裏で**別プロセスとして `claude` コマンド本体を起動**します。SDK自身は、そのCLIと標準入出力でJSONメッセージをやり取りするだけの、薄いラッパーです。

stdio で MCP サーバーを繋ぐと、プロセスはさらに二段深くなります。

```
① あなたのNode.jsスクリプト。例: npx tsx myscript.ts
    └─ ② claude CLIプロセス   ← 以後「CLI」と呼ぶ
         └─ ③ uv run main.py
              └─ ④ python3 main.py。MCPサーバー本体
```

API通信・ツールの実行判断・MCPサーバーとの接続管理をしているのは①ではなく**②のCLI**です。①が消えても②は自動的には消えません。これが後半の伏線になります。

先に、この記事で使う言葉を整理しておきます。

| 用語 | 意味 |
|---|---|
| `query()` | SDKが提供する関数。呼ぶたびに新しいCLIプロセスが1つ起動される |
| セッション | 1回の`query()`呼び出しに対応する、CLI⇔MCPサーバー間の一連のやり取り |
| CLI | SDKが裏で起動する`claude`コマンド本体。上図の② |
| stdio transport | クライアント(CLI)がサーバーをサブプロセスとして起動し、標準入出力でJSON-RPCをやり取りする通信方式 |
| SIGINT/SIGTERM | プロセスへ送る終了シグナル。ハンドラを登録して捕捉・無視できる |
| SIGKILL | 強制終了シグナル。カーネルレベルで強制され、捕捉も無視もできない唯一の手段 |
| 孤児プロセス | 親プロセスが死んだ後も生き残った子プロセス。init(PID 1)の下に付け替えられ、誰も後始末しなければ動き続ける |

## 検証方法:2軸を固定した構造化ハーネス

検証対象の軸は、思い切って2つだけに絞りました。

- **Trigger(終了トリガー)**: `normal-completion`(正常完了) / `interrupt`(Node.jsプロセス自身が`query.interrupt()`を呼び、CLIの実行中のターンを中断) / `subagent`(サブエージェント経由) / `kill-cli`(CLIを外部からSIGKILL) / `kill-node`(Node.jsホストを外部からSIGKILL)
- **ServerBehavior(MCPサーバーの行儀)**: `normal`(シグナル・EOFに素直に応答) / `ignore-signals`(シグナルだけ無視) / `ignore-signals-and-eof`(シグナルもEOFも無視して居座る)

この2軸**以外**の条件は、1箇所の定数に固定して全15シナリオで共有しています。MCPサーバーが公開するツールは`work(seconds)`という1つだけで、渡された秒数だけ`time.sleep()`でブロッキングするだけの単純な実装です。全シナリオで、この`work`ツールを同じ6秒という秒数で呼び出しています。ポーリング間隔は200ms、終了トリガー発火後の観察時間は一律90秒です。許可ツールや`permissionMode`も同様に固定しました。

同期も `sleep(N)` の当てずっぽうに頼らず、イベント駆動方式にしました。MCPサーバー自身が`work_started`などの構造化JSONLイベントを書き出すので、それを監視して同期を取っています。決定表も手で転記せず、`results/*.json` から機械的に生成しています。

## 結果:Trigger × ServerBehavior フルマトリクス(15/15)

実際に動かしてみると、こうなりました。表の「+N秒」は、どちらの列も**トリガーが発火した瞬間を0秒とした経過時間**です。cli列の`n/a`は「CLIプロセスが生き続けている」という意味ではありません。今回のハーネスは`kill-cli`/`kill-node`のときだけCLIプロセス自体の生死を追跡する作りになっており、それ以外のトリガーではCLIの生死を測定していないという意味です。CLIプロセスも「1 `query()` = 1 subprocess」の原則上、セッション終了時には必ず終了するはずですが、それが外部からのシグナルによるものか、CLI自身の自発的な終了なのかは、この記事では確認できていません。

| trigger | behavior | cli(claudeプロセス) | server(MCPサーバー) |
|---|---|---|---|
| normal-completion | normal | n/a | 死亡 (+0.00s) |
| normal-completion | ignore-signals | n/a | 死亡 (+0.00s) |
| normal-completion | ignore-signals-and-eof | n/a | 死亡 (+0.85s) |
| interrupt | normal | n/a | 死亡 (+0.00s) |
| interrupt | ignore-signals | n/a | 死亡 (+1.04s) |
| interrupt | ignore-signals-and-eof | n/a | 死亡 (+1.04s) |
| subagent | normal | n/a | 死亡 (+0.00s) |
| subagent | ignore-signals | n/a | 死亡 (+0.00s) |
| subagent | ignore-signals-and-eof | n/a | 死亡 (+0.83s) |
| kill-cli | normal | 死亡 (+0.20s) | 死亡 (+6.19s) |
| kill-cli | ignore-signals | 死亡 (+0.20s) | 死亡 (+6.15s) |
| kill-cli | ignore-signals-and-eof | 死亡 (+0.20s) | 死亡 (+6.20s) |
| kill-node | normal | 死亡 (+8.96s) | 死亡 (+8.56s) |
| kill-node | ignore-signals | 死亡 (+8.74s) | 死亡 (+8.33s) |
| kill-node | ignore-signals-and-eof | 死亡 (+12.65s) | 死亡 (+13.50s) |

cli列を左、server列を右に置いているのは「CLIへの操作が原因、MCPサーバーの死亡が結果」という向きに読めるようにするためです。ただし、この前後関係が常に成り立つわけではないことは先に触れておきます。詳しくは発見4で説明します。

ここまでとは別枠で、セッションとプロセスの対応関係も確認しました。メインNode.jsプロセスを終了させずに`query()`を2回連続実行すると、MCPサーバーは毎回**別PID**で立ち上がり直します。`query()`を2つ並列に実行しても、2つのMCPサーバーが**同時刻に**独立して生存します。stdio transportである以上、「1 `query()` = 1 MCPサーバープロセス = 1セッション」が仕様上の必然だからです。詳しくは後述します。

## 発見1: 行儀の良いサーバーなら、どのトリガーでも実質0秒で終わる

サーバーが素直にシグナル・stdin EOFへ応答する`normal`の場合に限れば、`normal-completion`/`interrupt`/`subagent`のいずれのトリガーでも、サーバープロセスは実質0秒で終了していました。最初のポーリングサンプルの時点で、すでに死亡していたということです。後始末のコードを自分で書く必要はなさそうでした。シグナルを無視する行儀の悪いサーバーだと話が変わってくるので、それは発見2で扱います。

サーバーに直接シグナルハンドラを仕込んで実測すると、**`SIGINT`と`SIGTERM`が1ミリ秒未満の差でほぼ同時に**届いていました。MCP公式仕様との比較は後述します。

## 発見2: 行儀の悪いサーバーにも、多段の安全網がある

`ignore-signals`、つまりシグナルだけを無視する行儀のとき、サーバーは**stdin EOFで自然終了**するため大きな遅れは出ませんでした。CLIはシグナル送信と並行してstdinのクローズも行っているようです。

`ignore-signals-and-eof`、つまりシグナルもEOFも無視する行儀のときは、サーバーのメインループが一度終了扱いになっても、内部でもう一度サーバーを起動し直す無限ループを仕込んであります。これは最強に強情な実装です。このサーバーには最終的に**`SIGKILL`**が飛んできました。詳細を追うと、シグナル送信から実際の強制終了まで約**5.5秒**でした。`SIGKILL`はカーネルレベルで強制されるシグナルなので、プロセス側に打つ手はありません。

ここで少し補足しておきます。表では`normal-completion × ignore-signals-and-eof`が0.85秒と、この5.5秒よりずっと短く見えますが、矛盾ではありません。サーバーのイベントログを見ると、シグナルが送られたタイミングと、`query()`が結果を返したタイミングにはずれがありました。CLIは**ターンの完了報告と、MCPサーバーの後始末を並行して**進めているようで、両者は同じ瞬間には終わらないのです。今回の表は、呼び出し側から見て`query()`が完了してから実際にプロセスが消えるまでを測っています。そのため、たまたま`query()`の完了タイミングが後始末の終盤、つまり強制終了の直前と重なっていた、というのが実態だと考えられます。**`query()`のfor-awaitループが終わった直後に「MCPサーバーはもう存在しない」と決めつけるのは、行儀の悪いサーバーの場合は安全ではなさそうです。**

## 発見3: `kill-cli`では、サーバーの死亡タイミングが「シグナル」ではなく「次にI/Oを試みるタイミング」で決まる

外部から`claude` CLIプロセスへ`kill -9`を撃ち込むと、3つの行儀すべてで、サーバーの死亡は**約6.15〜6.20秒後**とほぼ同一でした。これはワークツールの呼び出し秒数である6秒とほぼ一致します。

イベントログを見ると、この経路ではシグナルは一切送られていませんでした。サーバーは`time.sleep(6)`をブロッキングのまま最後まで実行し、完了後に結果を書き込もうとした瞬間にクラッシュしていました。壊れたパイプを検知したためです。`uv`も道連れに死んでいた理由は、このあと説明します。

つまり「行儀が良いか悪いか」は、少なくともこの経路では関係がなさそうです。`time.sleep()`のように何のI/Oも行わない処理を実行中は、シグナルハンドラの有無に関係なく、そのブロッキング処理が終わるまで異常を検知できません。サーバーの死亡タイミングは、概ね「トリガー発火時点で残っていた処理時間」で決まると言えそうです。

### プロセスツリーで見る:`uv`という隠れた安全装置

```
claude ← CLI。外部からkill -9
  └─ uv run --directory ... main.py   ← 自分の入出力が壊れたのを検知し、子を道連れに自滅
       └─ python3 main.py             ← MCPサーバー本体
```

`uv`自身のPIDを追跡すると、CLIが死んだ直後に`uv`と`python3`が**ほぼ同時**に消滅していました。差は0.3秒以内です。これは偶然ではなさそうです。[uv公式ドキュメント](https://docs.astral.sh/uv/concepts/projects/run/)にも「`uv`は`SIGKILL`/`SIGCHLD`/`SIGIO`/`SIGPOLL`を除くほとんどのシグナルを子プロセスへ転送する」と明記されており、意図された挙動の一事例だと考えられます。

ただし、万能ではなさそうです。[astral-sh/uv #12830](https://github.com/astral-sh/uv/issues/12830)では、`SIGHUP`/`SIGQUIT`/`SIGILL`/`SIGTRAP`/`SIGABRT`などでは**uvが転送せず自分だけ終了し、Pythonプロセスを孤児のまま残す**バグが報告されていました。「`uv`が常に安全装置になる」とは言い切れない点は、留意しておいたほうが良さそうです。

## 発見4: `kill-node`でも、CLIは自分で片付けてから終了する

今度はCLIの親、つまりNode.jsホストプロセス自体を外部から`kill -9`してみます。観察窓を一律**90秒**取って追跡したところ、CLIは`normal`/`ignore-signals`で約**8.7〜9.0秒後**、`ignore-signals-and-eof`でも約**12.7秒後**に、自分から終了していました。`ignore-signals-and-eof`だけやや長いのは、サーバー側の強制終了エスカレーションに要する約5.5秒の分、余計に時間がかかっているためで、これも一貫した説明がつきそうです。

CLIはホストプロセスが死んで孤児になっても、進行中のツール呼び出しが終わるのを待ち、通常のターン完了処理を一通り済ませたうえで、自分自身も終了しているようでした。この完了処理には、MCPサーバーへのシグナル送信や、応答がない場合の最終的な強制終了が含まれます。「孤児プロセスが無期限に残り続ける」というリスクは、少なくとも今回の90秒の観測範囲では確認されませんでした。

先ほどの表で触れた通り、CLIとサーバーどちらが先に死ぬかは、行儀によって入れ替わっていました。`normal`/`ignore-signals`ではサーバーの方が先に死に、`ignore-signals-and-eof`では逆にCLIの方が先に死んでいます。CLIは自分の後始末として、MCPサーバーへシグナルを送ります。このシグナル送信が終わるタイミングと、そのシグナルが実際にサーバーへ効くタイミングの前後関係は、サーバー側の応答の仕方によって変わるようです。

ただし、これは**ツール呼び出しの実行中というCLIが必ず活動している瞬間**にホストを落とした場合の結果です。CLIが次のプロンプトを待つだけの**アイドル状態**でホストが死んだ場合や、1つのセッション内で多数のサブエージェントを使い込んだ場合の挙動までは検証しておらず、そこは別の結果になる可能性が残っています。

逆に言えば、`uv`という安全装置が効くのは、あくまで**CLIの直接の子**が異常を検知できる場合に限られるようです。`uv`から見ると、CLIはまだ生きている親であり続けます。そのため、Node.jsが死んでも`uv`の安全装置はここでは発動しません。CLI自身が親の死に気づくかどうかは、その瞬間に何をしていたか次第だと言えそうです。

## 補足: killの対象プロセスの特定方法について

`kill-cli`/`kill-node`トリガーで対象の`claude` CLIプロセスを特定する際は、`ps -eo pid,ppid,cmd`からプロセスツリーをたどり、**自分が起動した子孫プロセスの中だけ**を探すようにしています。今回の検証環境はVS Code拡張のホストプロセス上で動作しており、他のセッションが同名バイナリを使っていることがあります。システム全体を対象にした`pgrep -f "claude-agent-sdk-.*/claude"`のようなパターンマッチだけに頼ると、そうした無関係なプロセスを誤って検出してしまう危険があるためです。候補が1つに定まらない場合はkillを見送り、結果に記録するだけにしています。

## MCP公式仕様・公式ドキュメントとの比較

[MCP公式仕様(2025-06-18)のLifecycle章](https://modelcontextprotocol.io/specification/2025-06-18/basic/lifecycle#shutdown)は、stdio transportのシャットダウン手順を次のように定めています。

> 1. まずサーバーへの入力ストリーム(stdin)を閉じる
> 2. サーバーの終了を待つ。合理的な時間内に終了しなければ`SIGTERM`
> 3. `SIGTERM`後も終了しなければ`SIGKILL`

発見1で見た通り、実測では「様子見」の段階は観測されず、`SIGINT`と`SIGTERM`がほぼ同時に送られていました。stdinのクローズもシグナルの送信と並行して行われているようです。仕様の"SHOULD"からは外れていますが、`SIGTERM`だけで毎回きれいに終了できている以上、実害のあるずれではなさそうです。

[Transports章 Session Management節](https://modelcontextprotocol.io/specification/2025-06-18/basic/transports#session-management)によれば、"session"の対応関係はtransportごとに異なります。stdioではセッションIDという概念自体が存在せず、「1回のサブプロセス起動 = 1セッション」が構造的に固定されます。今回のトポロジー確認の結果は、これと整合していました。逐次でも並列でも、毎回別PIDになっていたからです。

Claude Code / Agent SDKの公式ドキュメントも、あらためて確認してみました。

- **記載あり**: 「1 agent session = 1 subprocess」という粒度([Hosting the Agent SDK](https://code.claude.com/docs/en/agent-sdk/hosting)「The subprocess model」)、MCPサーバーの接続タイミングやステータス遷移([Connect to external tools with MCP](https://code.claude.com/docs/en/agent-sdk/mcp)「Connection timing」「Error handling」)
- **記載なし**: stdio MCPサーバーの子プロセスをいつ・どうやって終了させるか、ホストプロセスがクラッシュした場合の子プロセスの扱い

「1 query() = 1 subprocess」という設計思想は文書化されている一方、シグナルの種類・タイミング・トリガーやサーバーの行儀による違いは、今回のように自前のシグナルハンドラとプロセスツリー観察、構造化ハーネスによる検算でしか確かめられませんでした。

## 実務への示唆

冒頭で述べた通り、Claude Agent SDKでuvベースのstdio MCPサーバーは安心して使えそうです。そのうえで、実装するときに知っておくと役立ちそうなことを挙げておきます。

- **後始末は自分で書く必要はなさそうです。** 行儀の良い(`normal`)サーバーなら、どのトリガーでも子プロセスは実質0秒で終了します。
- **多少行儀の悪いMCPサーバーでも、ほぼ大丈夫そうです。** シグナルを無視するだけならstdin EOFが働きます。それも無視するならCLIの最終手段、約5.5秒後のSIGKILLが働きます。さらにCLI自体が消えても`uv run`自身の安全装置が働きます。ただしこれはI/O待ち中に限られ、効くとしても約1秒です。
- **`query()`が返ってきた直後に「MCPサーバーはもう消えた」と決めつけないほうが良さそうです。** 行儀の悪いサーバー相手だと、後始末が数秒バックグラウンドで続いていることがあります。
- **ブロッキングな長時間ツールは、親が死んでも自分では気づけません。** サーバーの死亡検知は「シグナル」ではなく「次にI/Oしようとするタイミング」で決まるようです。長時間処理を伴うツールを設計する際は、進捗通知などの定期的なI/Oを挟んでおくと、異常検知が速くなるかもしれません。
- **ホストプロセスがクラッシュしても、CLIは自分で片付けにいくようです。** ただし進行中の処理が終わるまで、長くて十数秒は生き続けます。短命なコンテナやサーバーレス環境では、この十数秒すら惜しい場面もあるはずなので、プロセスグループ単位で確実に後始末する監督者を用意しておくと安心かもしれません。
- **状態はセッションをまたいで残りません。** ここでいうセッションとは、1回の`query()`呼び出しに対応する単位のことです。stdio transportである以上、これは実装の都合ではなく仕様上の必然です。複数ターンにまたがる状態を持たせたいなら、MCPサーバー自体をStreamable HTTPの長命プロセスとして設計し直す必要がありそうです。

## まとめ

行儀の良いサーバーはもちろん、シグナルを無視するような行儀の悪いサーバーでも、子プロセスは自動的に片付いていました。シグナル、stdin EOF、`uv`の子プロセス連鎖終了、CLIの最終手段という何重かの安全網が働いているようです。ホストプロセスのクラッシュという一番厳しい条件でも、少なくとも「ツール呼び出し中に落ちた」場合は、無期限のリークにはなっていませんでした。

唯一気をつけたいのは、進行中の処理がI/Oを行わないブロッキング処理のときです。サーバーの死亡検知は処理が終わるまで遅れます。それと、CLIがアイドル中にホストが落ちた場合やサブエージェントを多用した場合は、今回はまだ検証できていません。

プロセスが「残るかどうか」は、結局のところ「その瞬間に何をしていたか」次第なのかもしれません。公式ドキュメントには書かれていないこの領域について、同じような疑問を持ったことがある方の参考に少しでもなれば嬉しいです。
