---
title: "stdio MCPサーバーの寿命 — Claude Agent SDKは子プロセスをいつ・どう終了させるか"
emoji: "🔌"
type: "tech"
topics: ["claudecode", "mcp", "typescript", "python", "uv"]
published: false
---

## この記事のきっかけ

TypeScript版のClaude Agent SDKで `mcpServers` に stdio 型の MCP サーバーを登録すると、SDK は裏で `claude` CLI を子プロセスとして起動します。その CLI が、さらに MCP サーバーをサブプロセスとして起動する、という構造です。この記事では、MCPサーバー側の実装として `uv run` で起動する Python サーバーを例に使います。

気になったのは、そのセッションが終わったときの後始末でした。正常終了でも、中断でも、あるいはホストプロセスがクラッシュしたときでも——この MCP サーバーの子プロセスは、誰が、どうやって片付けるのでしょうか。

[Agent SDK の公式ドキュメント](https://code.claude.com/docs/en/agent-sdk/mcp)を読んでみると、MCP サーバーの**接続**タイミングやステータス遷移は詳しく書かれています。一方で、子プロセスの終了のさせ方についての記載は見当たりませんでした。ここでいう「終了のさせ方」とは、シグナルの種類、タイミング、そして通常通りに終わらなかった場合にどうなるか、を指します。

そこで、この空白を実測で埋めてみることにしました。先に結論を言ってしまうと、**Claude Agent SDKでuvベースのstdio MCPサーバーは、特別な後始末コードを書かなくても安心して使えます**。セッションが正常に終わればMCPサーバーのプロセスもほぼ同時に終了しますし、サーバー側の実装が多少行儀の悪いものであっても、複数の仕組みが重なって最終的にはちゃんと片付きます。この記事では、その根拠になった実測データと、実装するうえで気をつけておきたいポイントを紹介します。

## 前提知識:誰が何を呼んでいるのか

`query()` を呼んでも、自分の Node.js コードがすべてをやっているわけではありません。SDK は裏で**別プロセスとして `claude` コマンド本体を起動**します。SDK自身は、そのCLIと標準入出力でJSONメッセージをやり取りするだけの、薄いラッパーです。

stdio で MCP サーバーを繋ぐと、プロセスはさらに二段深くなります。

```
① あなたのNode.jsスクリプト。例: npx tsx myscript.ts
    └─ ② claude CLIプロセス   ← 以後「CLI」と呼ぶ
         └─ ③ uv run main.py
              └─ ④ python3 main.py。MCPサーバー本体
```

API通信・ツールの実行判断・MCPサーバーとの接続管理をしているのは①ではなく**②のCLI**です。以後、①のNode.jsスクリプトを**ホストプロセス**と呼びます。ホストプロセスが消えても②は自動的には消えません。これが後半の伏線になります。

先に、この記事で使う言葉を整理しておきます。

| 用語 | 意味 |
|---|---|
| `query()` | SDKが提供する関数。呼ぶたびに新しいCLIプロセスが1つ起動される |
| セッション | 1回の`query()`呼び出しに対応する、CLI⇔MCPサーバー間の一連のやり取り |
| ホストプロセス | `query()`を呼ぶ側のNode.jsスクリプト。上図の① |
| CLI | SDKが裏で起動する`claude`コマンド本体。上図の② |
| サーバー、MCPサーバー | この記事で検証しているMCPサーバーのプロセスそのもの。上図の④(`python3 main.py`) |
| ツール | サーバーが公開している機能。CLIが呼び出す。この記事の検証では`work(seconds)`という1つだけを使う |
| ターン | ユーザーからの1回の入力に対して、CLIがツール呼び出しなどを経て応答を返すまでの一連のやり取り |
| stdio transport | クライアント(CLI)がサーバーをサブプロセスとして起動し、標準入出力でJSON-RPCをやり取りする通信方式 |
| SIGINT/SIGTERM | プロセスへ送る終了シグナル。ハンドラを登録して捕捉・無視できる |
| SIGKILL | 強制終了シグナル。カーネルレベルで強制され、捕捉も無視もできない唯一の手段 |
| プロセスグループ | 複数のプロセスをまとめて1つの単位として扱うOSの仕組み。シグナルをグループ単位で送ることができる |
| 孤児プロセス | 親プロセスが死んだ後も生き残った子プロセス。init(PID 1)の下に付け替えられ、誰も後始末しなければ動き続ける |

## 検証方法:2軸を固定した構造化ハーネス

検証対象の軸は、思い切って2つだけに絞りました。

- **Trigger(終了トリガー)**: `normal-completion`(正常完了) / `interrupt`(ホストプロセス自身が`query.interrupt()`を呼び、CLIの実行中のターンを中断) / `subagent`(CLIが直接ツールを呼ぶのではなく、Taskツールで起動したサブエージェントにツール呼び出しをやらせる) / `kill-cli`(CLIを外部からSIGKILL) / `kill-node`(ホストプロセスを外部からSIGKILL)
- **ServerBehavior(MCPサーバーの行儀)**: `normal`(シグナル・EOFに素直に応答) / `ignore-signals`(シグナルだけ無視) / `ignore-signals-and-eof`(シグナルもEOFも無視して居座る)

この2軸**以外**の条件は、1箇所の定数に固定して全15シナリオで共有しています。MCPサーバーが公開するツールは`work(seconds)`という1つだけで、渡された秒数だけ`time.sleep()`でブロッキングするだけの単純な実装です。全シナリオで、この`work`ツールを同じ6秒という秒数で呼び出しています。ポーリング間隔は200ms、終了トリガー発火後の観察時間は一律90秒です。許可ツールや`permissionMode`も同様に固定しました。

同期も `sleep(N)` の当てずっぽうに頼らず、イベント駆動方式にしました。MCPサーバー自身が`work_started`などの構造化JSONLイベントを書き出すので、それを監視して同期を取っています。決定表も手で転記せず、`results/*.json` から機械的に生成しています。

## 結果:Trigger × ServerBehavior フルマトリクス(15/15、各セル20回試行)

単発の測定では、たまたま出た1つの値が典型的なのか外れ値なのか区別がつきません。そこで全15セルをそれぞれ20回ずつ実行し(合計300回)、平均・中央値・標準偏差(σ)を算出し直しました。表の値は、どちらの列も**トリガーが発火した瞬間を0秒とした経過時間**の平均・中央値です。

前回の測定では、CLIプロセス自体の生死は`kill-cli`/`kill-node`のときしか追跡できておらず、それ以外のトリガーではcli列を`n/a`にしていました。今回はこの制約を解消し、全トリガーで`work_started`イベントの時点からCLIプロセスのPIDを特定し、サーバーと同じ精度で生死を追跡しています。この記事の`n/a`は「測定したかったが技術的に測定不可能だった」場合だけに使うことにしており、今回の300回ではそれに該当するセルはありませんでした。

| trigger | behavior | cli(claudeプロセス) | server(MCPサーバー) |
|---|---|---|---|
| normal-completion | normal | 平均0.00s ± 0.00s (中央値0.00s) | 平均0.00s ± 0.00s (中央値0.00s) |
| normal-completion | ignore-signals | 平均0.00s ± 0.00s (中央値0.00s) | 平均0.00s ± 0.00s (中央値0.00s) |
| normal-completion | ignore-signals-and-eof | 平均0.00s ± 0.00s (中央値0.00s) | 平均0.78s ± 0.09s (中央値0.80s) |
| interrupt | normal | 平均0.00s ± 0.00s (中央値0.00s) | 平均0.45s ± 0.57s (中央値0.00s) |
| interrupt | ignore-signals | 平均0.00s ± 0.00s (中央値0.00s) | 平均1.09s ± 0.10s (中央値1.01s) |
| interrupt | ignore-signals-and-eof | 平均0.00s ± 0.00s (中央値0.00s) | 平均1.08s ± 0.10s (中央値1.00s) |
| subagent | normal | 平均0.00s ± 0.00s (中央値0.00s) | 平均0.00s ± 0.00s (中央値0.00s) |
| subagent | ignore-signals | 平均0.00s ± 0.00s (中央値0.00s) | 平均0.00s ± 0.00s (中央値0.00s) |
| subagent | ignore-signals-and-eof | 平均0.00s ± 0.00s (中央値0.00s) | 平均0.80s ± 0.01s (中央値0.80s) |
| kill-cli | normal | 平均0.20s ± 0.00s (中央値0.20s) | 平均6.29s ± 0.33s (中央値6.21s) |
| kill-cli | ignore-signals | 平均0.20s ± 0.00s (中央値0.20s) | 平均6.29s ± 0.33s (中央値6.21s) |
| kill-cli | ignore-signals-and-eof | 平均0.20s ± 0.00s (中央値0.20s) | 平均6.21s ± 0.02s (中央値6.21s) |
| kill-node | normal | 平均9.11s ± 0.27s (中央値9.06s) | 平均8.57s ± 0.30s (中央値8.52s) |
| kill-node | ignore-signals | 平均8.80s ± 0.35s (中央値8.82s) | 平均8.26s ± 0.34s (中央値8.22s) |
| kill-node | ignore-signals-and-eof | 平均12.68s ± 0.45s (中央値12.63s) | 平均13.42s ± 0.48s (中央値13.23s) |

cli列を左、server列を右に置いているのは「CLIへの操作が原因、MCPサーバーの死亡が結果」という向きに読めるようにするためです。ただし、この前後関係が常に成り立つわけではないことは先に触れておきます。詳しくは発見4で説明します。

ここまでとは別枠で、セッションとプロセスの対応関係も確認しました。ホストプロセスを終了させずに`query()`を2回連続実行すると、MCPサーバーは毎回**別PID**で立ち上がり直します。`query()`を2つ並列に実行しても、2つのMCPサーバーが**同時刻に**独立して生存します。stdio transportである以上、「1 `query()` = 1 MCPサーバープロセス = 1セッション」が仕様上の必然だからです。詳しくは後述します。

## 発見1: 行儀の良いサーバーなら、どのトリガーでもCLI・サーバーとも実質0秒で終わる

サーバーが素直にシグナル・stdin EOFへ応答する`normal`の場合、`normal-completion`と`subagent`のトリガーでは、サーバープロセスは20回中20回とも実質0秒(σ=0.00s)で終了していました。最初のポーリングサンプルの時点で、すでに死亡していたということです。`interrupt`だけは少し違い、平均0.45秒・σ=0.57秒で、0.00秒で終わる回と1.20秒かかる回の両方がありました。このばらつきの理由は発見2で扱う内容と同じで、`interrupt`によるセッション終了はMCPサーバーの後始末を律儀に待たない場合がある、という傾向の一部だと考えられます。

今回はCLIプロセス自身の生死も同じ精度で追跡できるようになったので、あわせて確認しました。結果は、サーバーの行儀に関わらず、`normal-completion`/`interrupt`/`subagent`のいずれのトリガーでもCLIは20回とも実質0秒(σ=0.00s)で消えていました。つまりCLIは、`query()`のfor-awaitループが完了を返す時点で、すでにOSプロセスとしては存在していないということです。「1 `query()` = 1 subprocess」という設計が、外から観察しても矛盾なく成立していました。後始末のコードを自分で書く必要はなさそうでした。シグナルを無視する行儀の悪いサーバーだと話が変わってくるので、それは発見2で扱います。

サーバーに直接シグナルハンドラを仕込んで実測すると、**`SIGINT`と`SIGTERM`が1ミリ秒未満の差でほぼ同時に**届いていました。MCP公式仕様との比較は後述します。

## 発見2: 行儀の悪いサーバーにも、多段の安全網がある。ただし遅れ方はトリガーによって違う

`ignore-signals`、つまりシグナルだけを無視する行儀のとき、サーバーは**stdin EOFで自然終了**するため大きな遅れは出ません。ただし、その遅れ方はトリガーによって違いました。`normal-completion`と`subagent`ではサーバーは20回とも実質0秒で終了していた一方、`interrupt`だけは平均**1.09秒**(σ=0.10s)かかっていました。`query.interrupt()`によるセッション終了は、CLIがMCPサーバーのstdin EOFへの応答を待ちきる前に、ホスト側へ完了を返しているようです。CLIはシグナル送信と並行してstdinのクローズも行っているようですが、その後片付けの完了を`interrupt()`が律儀に待つとは限らない、ということだと考えられます。

`ignore-signals-and-eof`、つまりシグナルもEOFも無視する行儀のときは、サーバーのメインループが一度終了扱いになっても、内部でもう一度サーバーを起動し直す無限ループを仕込んであります。これは最強に強情な実装です。このサーバーには最終的に**`SIGKILL`**が飛んできました。詳細を追うと、シグナル送信から実際の強制終了まで約**5.5秒**でした。`SIGKILL`はカーネルレベルで強制されるシグナルなので、プロセス側に打つ手はありません。

ここで少し補足しておきます。表では`normal-completion × ignore-signals-and-eof`が平均0.78秒、`subagent × ignore-signals-and-eof`が平均0.80秒と、この5.5秒よりずっと短く見えますが、矛盾ではありません。サーバーのイベントログを見ると、シグナルが送られたタイミングと、`query()`が結果を返したタイミングにはずれがありました。CLIは**ターンの完了報告と、MCPサーバーの後始末を並行して**進めているようで、両者は同じ瞬間には終わらないのです。今回の表は、呼び出し側から見て`query()`が完了してから実際にプロセスが消えるまでを測っています。そのため、たまたま`query()`の完了タイミングが後始末の終盤、つまり強制終了の直前と重なっていた、というのが実態だと考えられます。`interrupt`トリガーだけは、この`ignore-signals-and-eof`でも平均1.08秒(σ=0.10s)と、`ignore-signals`とほぼ同じ値でした。ここでも`interrupt`によるセッション終了が、MCPサーバーの後始末の完了を待たずに返っている可能性が高そうです。**`query()`のfor-awaitループが終わった直後に「MCPサーバーはもう存在しない」と決めつけるのは、行儀の悪いサーバーの場合は安全ではなさそうです。**

## 発見3: `kill-cli`では、サーバーの死亡タイミングが「シグナル」ではなく「次にI/Oを試みるタイミング」で決まる

外部から`claude` CLIプロセスへ`kill -9`を撃ち込むと、3つの行儀すべてで、サーバーの死亡は**平均6.2〜6.3秒後**とほぼ同一でした(中央値はいずれも6.21秒)。これはワークツールの呼び出し秒数である6秒とほぼ一致します。

イベントログを見ると、この経路ではシグナルは一切送られていませんでした。サーバーは`time.sleep(6)`をブロッキングのまま最後まで実行し、完了後に結果を書き込もうとした瞬間にクラッシュしていました。壊れたパイプを検知したためです。`uv`も道連れに死んでいた理由は、このあと説明します。

つまり「行儀が良いか悪いか」は、少なくともこの経路では関係がなさそうです。`time.sleep()`のように何のI/Oも行わない処理を実行中は、シグナルハンドラの有無に関係なく、そのブロッキング処理が終わるまで異常を検知できません。サーバーの死亡タイミングは、概ね「トリガー発火時点で残っていた処理時間」で決まると言えそうです。

### プロセスツリーで見る:`uv`という隠れた安全装置

```
claude ← CLI。外部からkill -9
  └─ uv run --directory ... main.py   ← 自分の入出力が壊れたのを検知し、子を道連れに自滅
       └─ python3 main.py             ← MCPサーバー本体
```

`uv`自身のPIDを追跡すると、CLIが死んだ直後に`uv`と`python3`が**ほぼ同時**に消滅していました。差は0.3秒以内です。これは偶然ではなさそうです。[uv公式ドキュメント](https://docs.astral.sh/uv/concepts/projects/run/)にも「`uv`は`SIGKILL`/`SIGCHLD`/`SIGIO`/`SIGPOLL`を除くほとんどのシグナルを子プロセスへ転送する」と明記されており、意図された挙動の一事例だと考えられます。

ただし、万能ではなさそうです。[astral-sh/uv #12830](https://github.com/astral-sh/uv/issues/12830)では、`SIGHUP`/`SIGQUIT`/`SIGILL`/`SIGTRAP`/`SIGABRT`などでは**uvが転送せず自分だけ終了し、Pythonプロセスを孤児のまま残す**バグが報告されていました。「`uv`が常に安全装置になる」とは言い切れない点は、留意しておいたほうが良さそうです。

## 発見4: `kill-node`でも、CLIは自分で片付けてから終了する

今度はCLIの親、つまりホストプロセス自体を外部から`kill -9`してみます。観察窓を一律**90秒**取って追跡したところ、CLIは`normal`/`ignore-signals`で平均**約8.8〜9.1秒後**、`ignore-signals-and-eof`でも平均**約12.7秒後**に、自分から終了していました(いずれもσは0.3〜0.5秒程度で、20回とも近い値に収まっています)。`ignore-signals-and-eof`だけやや長いのは、サーバー側の強制終了エスカレーションに要する約5.5秒の分、余計に時間がかかっているためで、これも一貫した説明がつきそうです。

CLIはホストプロセスが死んで孤児になっても、進行中のツール呼び出しが終わるのを待ち、通常のターン完了処理を一通り済ませたうえで、自分自身も終了しているようでした。この完了処理には、MCPサーバーへのシグナル送信や、応答がない場合の最終的な強制終了が含まれます。「孤児プロセスが無期限に残り続ける」というリスクは、少なくとも今回の90秒の観測範囲では確認されませんでした。

先ほどの表で触れた通り、CLIとサーバーどちらが先に死ぬかは、行儀によって入れ替わっていました。`normal`/`ignore-signals`ではサーバーの方が先に死に、`ignore-signals-and-eof`では逆にCLIの方が先に死んでいます。CLIは自分の後始末として、MCPサーバーへシグナルを送ります。このシグナル送信が終わるタイミングと、そのシグナルが実際にサーバーへ効くタイミングの前後関係は、サーバー側の応答の仕方によって変わるようです。

ただし、これは**ツール呼び出しの実行中というCLIが必ず活動している瞬間**にホストプロセスを落とした場合の結果です。CLIが次のプロンプトを待つだけの**アイドル状態**でホストプロセスが死んだ場合や、1つのセッション内で多数のサブエージェントを使い込んだ場合の挙動までは検証しておらず、そこは別の結果になる可能性が残っています。

逆に言えば、`uv`という安全装置が効くのは、あくまで**CLIの直接の子**が異常を検知できる場合に限られるようです。`uv`から見ると、CLIはまだ生きている親であり続けます。そのため、ホストプロセスが死んでも`uv`の安全装置はここでは発動しません。CLI自身が親の死に気づくかどうかは、その瞬間に何をしていたか次第だと言えそうです。

## 補足: killの対象プロセスの特定方法について

`kill-cli`/`kill-node`トリガーで対象の`claude` CLIプロセスを特定する際は、`ps -eo pid,ppid,cmd`からプロセスツリーをたどり、**自分が起動した子孫プロセスの中だけ**を探すようにしています。今回の検証環境はVS Code拡張の実行環境上で動作しており、他のセッションが同名バイナリを使っていることがあります。システム全体を対象にした`pgrep -f "claude-agent-sdk-.*/claude"`のようなパターンマッチだけに頼ると、そうした無関係なプロセスを誤って検出してしまう危険があるためです。候補が1つに定まらない場合はkillを見送り、結果に記録するだけにしています。

## MCP公式仕様・公式ドキュメントとの比較

[MCP公式仕様(2025-06-18)のLifecycle章](https://modelcontextprotocol.io/specification/2025-06-18/basic/lifecycle#shutdown)は、stdio transportのシャットダウン手順を次のように定めています。

> 1. まずサーバーへの入力ストリーム(stdin)を閉じる
> 2. サーバーの終了を待つ。合理的な時間内に終了しなければ`SIGTERM`
> 3. `SIGTERM`後も終了しなければ`SIGKILL`

発見1で見た通り、実測では「様子見」の段階は観測されず、`SIGINT`と`SIGTERM`がほぼ同時に送られていました。stdinのクローズもシグナルの送信と並行して行われているようです。仕様の"SHOULD"からは外れていますが、`SIGTERM`だけで毎回きれいに終了できている以上、実害のあるずれではなさそうです。

[Transports章 Session Management節](https://modelcontextprotocol.io/specification/2025-06-18/basic/transports#session-management)によれば、"session"の対応関係はtransportごとに異なります。stdioではセッションIDという概念自体が存在せず、「1回のサブプロセス起動 = 1セッション」が構造的に固定されます。今回のトポロジー確認の結果は、これと整合していました。逐次でも並列でも、毎回別PIDになっていたからです。

Claude Code / Agent SDKの公式ドキュメントも、あらためて確認してみました。

- **記載あり**: 「1 agent session = 1 subprocess」という粒度([Hosting the Agent SDK](https://code.claude.com/docs/en/agent-sdk/hosting)「The subprocess model」)、MCPサーバーの接続タイミングやステータス遷移([Connect to external tools with MCP](https://code.claude.com/docs/en/agent-sdk/mcp)「Connection timing」「Error handling」)
- **記載なし**: stdio MCPサーバーの子プロセスをいつ・どうやって終了させるか、ホストプロセスがクラッシュした場合の子プロセスの扱い

「1 query() = 1 subprocess」という設計思想は文書化されている一方、シグナルの種類・タイミング・トリガーやサーバーの行儀による違いは、今回のように自前のシグナルハンドラとプロセスツリー観察、構造化ハーネスによる検算でしか確かめられませんでした。

## 実務への示唆

冒頭で述べた通り、Claude Agent SDKでuvベースのstdio MCPサーバーは安心して使えそうです。ここでは、それぞれの立場で具体的に何をすればいいかを書いておきます。

### 使う側:後始末のコードは要らない

MCPサーバーを`mcpServers`に登録して使うだけの立場なら、書くコードはこれだけで十分そうです。

```typescript
for await (const msg of query({ prompt, options: { mcpServers } })) {
  // 通常の処理
}
// ループを抜けた時点で、行儀の良いサーバーならもう子プロセスは消えている
```

`finally`節でMCPサーバーのプロセスを`kill`するようなコードは、行儀の良いサーバー相手には不要そうです。ただし発見2で見た通り、ループを抜けた直後に「もうプロセスは無い」と決めつけて、ロックファイルの削除やポートの再利用のような処理をすぐ続けるのは避けたほうが良さそうです。心配なら、プロセスの生死を明示的に確認してから進めることもできます。

```typescript
function isAlive(pid: number): boolean {
  try { process.kill(pid, 0); return true; } catch { return false; }
}
```

### サーバーを自作する側:ブロッキング処理には定期的なI/Oを挟む

発見3で見た通り、死亡タイミングは「シグナルを受け取った瞬間」ではなく「次にI/Oを試みる瞬間」で決まるようです。ツールの実装が長時間ブロッキングし続けると、その間はCLIとの接続が切れても気づけません。

```python
# 気づきが遅れる例。6秒間、何のI/Oもしない
@mcp.tool()
def long_task(seconds: int) -> str:
    time.sleep(seconds)
    return "done"
```

```python
# 気づきが早い例。1秒ごとに区切ってI/Oの機会を作る
@mcp.tool()
async def long_task(seconds: int, ctx: Context) -> str:
    for i in range(seconds):
        await asyncio.sleep(1)
        await ctx.report_progress(i + 1, seconds)
    return "done"
```

進捗通知のAPIが無い実装でも、ループを短く区切るだけで、気づきの遅れを最大でも1秒程度に縮められそうです。

### サーバーを自作する側:SIGTERMハンドラは自分で用意する

発見3で見つけた`uv`の安全装置は、あくまで`uv`というランチャー固有の挙動でした。他のランチャーで同じ安全網が働く保証はありません。自作サーバーなら、`SIGTERM`ハンドラくらいは自分で実装しておくのが本筋です。

```python
import signal, sys

def handle_sigterm(signum, frame):
    # 進行中の処理があれば中断・後片付け
    sys.exit(0)

signal.signal(signal.SIGTERM, handle_sigterm)
```

### 動かす側:ホストプロセスのクラッシュだけは運用で手当てする

発見4で見た通り、ホストプロセスがクラッシュしても、CLIは孤児化した状態から自分で片付けにいくようでした。ただし進行中の処理が終わるまで、長くて十数秒は生き続けます。短命なコンテナやサーバーレス環境では、この十数秒すら惜しい場面がありそうです。

- Dockerなら、`docker run --init`を付けておくと、孤児プロセスを誰も回収しない問題をtiniが肩代わりしてくれます。Docker Composeなら`init: true`が同じ役割です
- systemdなら、ユニットファイルに`KillMode=control-group`を指定しておくと、メインプロセスだけでなく子孫プロセスもまとめて確実にkillされます
- コンテナオーケストレータを使わず素のVMで動かす場合は、起動前に前回セッションの残骸を掃除しておく手もあります

```bash
pkill -f "uv run --directory .*/your-mcp-server" 2>/dev/null; true
```

### 設計する側:セッションをまたぐ状態は外に置く

ホストプロセスを維持したまま`query()`を2回連続・あるいは並列で実行しても、MCPサーバーは毎回別PIDで立ち上がり直していました。プロセスが別である以上、メモリ上に持たせた状態は引き継がれません。

```python
# stdioでは意味がない。query()のたびにプロセスごと作り直される
_connection_pool = create_pool()
```

複数ターンにまたがる状態を持たせたいなら、状態そのものをRedisやファイルなどプロセスの外に置くか、あるいはMCPサーバー自体をStreamable HTTPの長命プロセスとして設計し直し、`Mcp-Session-Id`ごとに状態を分離する必要がありそうです。

## まとめ

使う側にとっては、ほぼ何もしなくていい、というのが今回の一番の結論でした。行儀の良いサーバーはもちろん、シグナルを無視するような行儀の悪いサーバーでも、子プロセスは自動的に片付いていました。シグナル、stdin EOF、`uv`の子プロセス連鎖終了、CLIの最終手段という何重かの安全網が働いているようです。ホストプロセスのクラッシュという一番厳しい条件でも、少なくとも「ツール呼び出し中に落ちた」場合は、無期限のリークにはなっていませんでした。

一方で、サーバーを作る側と動かす側には、それぞれ気をつけたいことが残りました。ブロッキング処理には定期的なI/Oを挟むこと、そしてホストプロセスのクラッシュだけは運用側でプロセスグループごと後始末する仕組みを用意しておくことです。CLIがアイドル中にホストプロセスが落ちた場合や、サブエージェントを多用した場合は、今回はまだ検証できていません。

プロセスが「残るかどうか」は、結局のところ「その瞬間に何をしていたか」次第なのかもしれません。公式ドキュメントには書かれていないこの領域について、同じような疑問を持ったことがある方の参考に少しでもなれば嬉しいです。
